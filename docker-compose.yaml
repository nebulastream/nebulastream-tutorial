services:

  nes:
    image: nebulastream/worker:main
    volumes:
      - ./output:/output

  datagen:
    build:
      context: docker/datagen

  nebuli:
    build:
      context: docker/nebuli
      dockerfile: Dockerfile.nebuli
    volumes:
      - ./queries:/queries