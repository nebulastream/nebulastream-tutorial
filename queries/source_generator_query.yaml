query: |
  SELECT ID, FIELD2
  FROM Generator(
      'ALL' as `SOURCE`.STOP_GENERATOR_WHEN_SEQUENCE_FINISHES,
      1000000 AS `SOURCE`.MAX_RUNTIME_MS,
      1 AS `SOURCE`.SEED,
      'CSV' AS PARSER.`TYPE`,
      'SEQUENCE UINT64 0 10 1, SEQUENCE UINT64 0 5 1' AS `SOURCE`.GENERATOR_SCHEMA,
      SCHEMA(id UINT64, field2 UINT64) AS `SOURCE`.`SCHEMA`)
  INTO File('CSV' AS `SINK`.INPUT_FORMAT, "/output/test2.csv" AS `SINK`.FILE_PATH)

sinks: []
logical: []
physical: []
